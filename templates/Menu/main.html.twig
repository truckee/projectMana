<nav class="navbar navbar-expand-md navbar-light bg-light mb-3 border">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path('home') }}">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarContacts" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          New contacts
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarContacts">
          <a class="dropdown-item" href="{{ path('contacts_add', {'source':'Most recent'}) }}">Most recent</a>
          <a class="dropdown-item" href="{{ path('contacts_add', {'source':'FY to date'}) }}">FY to date</a>
        </div>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('household_new') }}">New household</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('report_menu') }}">Reports</a>
      </li>
      {% if is_granted('ROLE_ADMIN') %}
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarAdmin" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Admin
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarAdmin">
          <a class="dropdown-item" href="{{ path('user_invitation') }}">Invite user</a>
          <a class="dropdown-item" href="{{ path('easyadmin') }}">Options & users</a>
          <a class="dropdown-item" href="{{ path('status') }}">Change status</a>
        </div>
      </li>
      {% endif %}
    </ul>
    <form class="form-inline my-2 my-lg-0" action="{{ path('_search') }}">
      <input class="form-control" id="household_search" name="qtext" type="search" placeholder="Name or id" aria-label="Search">
      <button class="btn border my-2 my-sm-0 bg-white" id="_search" type="submit"><span class="fa fa-search"></span></button>
    </form>
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarAdmin" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Account
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarAccount">
          <a class="dropdown-item" href="{{ path('reset_password') }}">Change password</a>
          <a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
        </div>
      </li>
    </ul>
  </div>
</nav>