<div class="card">
    <div class="card-header bg-info">Members</div>
    <div id="members">
        {% for member in household.members %}
            {{ include('Member/memberShowBlock_content.html.twig') }}
        {% endfor %}
    </div>
    <div id="memberEditDialog"></div>
    <div class="card-body">
        <a id="addMember" href="#" class="btn-sm btn-info border">Add member</a>
    </div>
</div>
